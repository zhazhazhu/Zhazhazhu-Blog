export interface userModel {
  userName: string;
  passWord: string
}

export interface userDataModel {
  userName: string;
  passWord: string;
  email: string;
  avatar: string;
  phoneNumber: string;
}